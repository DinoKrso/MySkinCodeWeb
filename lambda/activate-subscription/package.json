{
  "name": "activate-subscription",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Lambda: aktivacija pretplate nakon Monri uplate",
  "main": "handler.mjs",
  "scripts": {
    "zip": "npm install --omit=dev && zip -r activate-subscription.zip handler.mjs node_modules package.json"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.758.0",
    "@aws-sdk/lib-dynamodb": "^3.758.0"
  }
}
